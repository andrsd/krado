# Notes for netgen

- version 6.2.2505
    - hardcoded in `netgen_version.hpp.in`
- `netgen_config.hpp` is empty, because it is included,
  but we do not use the #define macros. We use cmake
  to pass compile-time defines into the build process
- `netgen/libsrc/rules` was generated by running netgen
  cmake build - it is a generated code. We do not do that.
  We just use the files.
- `netgen/netgen.cpp` has some variables that are global
  in netgen driver and the lib references them, so we
  just supply that to be able to link our dylib.
